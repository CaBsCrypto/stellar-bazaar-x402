import {NextRequest,NextResponse} from "next/server";import {services} from "@/lib/catalog";import {filterServices} from "@/lib/discovery";import {toServiceCard} from "@/lib/service-card";
export function GET(req:NextRequest){const p=req.nextUrl.searchParams;const results=filterServices(services,{kind:p.get("kind")??undefined,scheme:p.get("scheme")??undefined,asset:p.get("asset")??undefined,network:p.get("network")??undefined,maxPrice:p.get("maxPrice")?Number(p.get("maxPrice")):undefined});return NextResponse.json({results:results.map(toServiceCard),count:results.length,cursor:null,indexStatus:"local-mvp"})}
