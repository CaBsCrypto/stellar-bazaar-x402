import{NextResponse}from"next/server";import{createEd25519Signer}from"@x402/stellar";import{ExactStellarScheme}from"@x402/stellar/exact/client";import{x402Client}from"@x402/core/client";import{wrapFetchWithPayment}from"@x402/fetch";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function POST(){if(process.env.X402_ENABLE_LOCAL_PAYER!=="true")return NextResponse.json({ok:false,error:{code:"LOCAL_PAYER_DISABLED",message:"Demo payer sólo disponible en entorno local explícitamente habilitado.",retryable:false}},{status:403});const secret=process.env.X402_PAYER_SECRET;if(!secret)return NextResponse.json({ok:false,error:{code:"PAYER_SECRET_MISSING",message:"Seed Testnet no configurada server-side.",retryable:false}},{status:503});try{const signer=createEd25519Signer(secret,"stellar:testnet"),client=new x402Client().register("stellar:testnet",new ExactStellarScheme(signer)),paidFetch=wrapFetchWithPayment(fetch,client),base=process.env.X402_RESOURCE_BASE_URL??"http://127.0.0.1:3210",response=await paidFetch(`${base}/api/x402/swap-risk?pair=XLM%2FUSDC&amount=2500&side=buy`),body=await response.json();return NextResponse.json({status:response.status,paymentResponsePresent:response.headers.has("payment-response"),body},{status:response.status})}catch{return NextResponse.json({ok:false,error:{code:"TESTNET_PAYMENT_FAILED",message:"Pago Testnet no completado; revisa saldo USDC/configuración server-only.",retryable:true}},{status:402})}}
