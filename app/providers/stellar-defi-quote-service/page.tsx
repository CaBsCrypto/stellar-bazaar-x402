import Link from"next/link";import{EXTERNAL_QUOTE_MANIFEST,getExternalQuoteCard}from"@/lib/external-provider";
export default function ExternalQuoteProvider(){const card=getExternalQuoteCard();return <main><div className="mock-banner">EXTERNAL CONTRACT · READ-ONLY · PROVIDER NOT DEPLOYED / CONTRATO EXTERNO · PROVEEDOR NO DESPLEGADO</div><nav className="nav shell"><Link href="/" className="brand"><span>✦</span> Stellar Bazaar <sup>x402</sup></Link><Link href="/#catalogo">← Catálogo / Catalogue</Link><span className="network-pill"><i/> Contract only</span></nav><section className="shell publish-hero"><span className="kicker">EXTERNAL PROVIDER / PROVEEDOR EXTERNO</span><h1>{card.title.es}<br/><em>{card.title.en}</em></h1><p>{card.description.es}<br/>{card.description.en}</p><div className="provider-promise"><span>HTTP POST</span><span>Deterministic fixture</span><span>No shared code</span><span>No payment active</span></div></section><section className="shell publish-section"><div className="definition-grid"><article className="yes"><span>PUBLIC CONTRACT</span><strong>POST /api/quote</strong><p>Inputs: sellAsset, buyAsset, amount. Structured deterministic output. The provider repository remains independent.</p><a className="text-link" href={EXTERNAL_QUOTE_MANIFEST}>Manifest público / Public manifest ↗</a></article><article className="no"><span>CURRENT LIMIT</span><strong>No public endpoint or x402 yet</strong><p>Sin endpoint desplegado, 402, settlement o transporte MCP publicado. Bazaar no simula disponibilidad.</p></article></div></section></main>}
