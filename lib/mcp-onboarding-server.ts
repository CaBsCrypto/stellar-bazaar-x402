import{McpServer}from"@modelcontextprotocol/sdk/server/mcp.js";import*as z from"zod/v4";import{services}from"./catalog";import{rankServices,validateServiceCard}from"./discovery";import{toServiceCard}from"./service-card";import{pilotCapabilityCard,pilotCards}from"./pilot-cards";
const result=(value:unknown)=>({content:[{type:"text" as const,text:JSON.stringify(value)}],structuredContent:value as Record<string,unknown>}),notFound=(id:string)=>({isError:true,content:[{type:"text" as const,text:JSON.stringify({code:"RESOURCE_NOT_FOUND",message:`Unknown service id: ${id}`,retryable:false,stage:"discover"})}]});
export function createOnboardingMcpServer(){const server=new McpServer({name:"stellar-bazaar-discovery",version:"0.2.0"});
 server.registerTool("get_bazaar_capabilities",{description:"Read-only capability and policy card.",inputSchema:{}},async()=>result(pilotCapabilityCard));
 server.registerTool("list_services",{description:"List active local service cards. Pilot fixtures are excluded unless includePilots is true.",inputSchema:{includePilots:z.boolean().optional()}},async({includePilots})=>result({services:services.map(service=>({...toServiceCard(service),availability:{execution:service.id==="swap-risk-quote"?"active-local":"fixture-only",payment:service.id==="swap-risk-quote"?"testnet-validated":"not-active"}})),pilots:includePilots?pilotCards:[],partialResults:false}));
 server.registerTool("search_services",{description:"Deterministic lexical search over current service cards; no AI/reputation inference.",inputSchema:{query:z.string().min(1)}},async({query})=>result({query,ranking:{version:"lexical-v1",ai:false},results:rankServices(services,query).map(entry=>({resource:toServiceCard(entry.service),score:entry.score,reasons:entry.reasons})),partialResults:false}));
 server.registerTool("get_service",{description:"Inspect a service or pilot by machine-stable ID.",inputSchema:{id:z.string().min(1)}},async({id})=>{const service=services.find(item=>item.id===id);if(service)return result({resource:toServiceCard(service),availability:{execution:id==="swap-risk-quote"?"active-local":"fixture-only",payment:id==="swap-risk-quote"?"testnet-validated":"not-active"}});const pilot=pilotCards.find(item=>item.id===id);return pilot?result({resource:pilot}):notFound(id)});
 server.registerTool("validate_service_card",{description:"Deterministic shape checks only; never provider certification.",inputSchema:{card:z.record(z.string(),z.unknown())}},async({card})=>{try{const outcomes=validateServiceCard(card as never);return result({valid:!outcomes.some(x=>x.status==="fail"),outcomes,certification:false})}catch{return result({valid:false,outcomes:[{rule:"schema.shape",status:"fail",reason:"Malformed service card"}],certification:false})}});return server}
