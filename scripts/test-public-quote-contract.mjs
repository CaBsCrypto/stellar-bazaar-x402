import assert from"node:assert/strict";
const url="https://raw.githubusercontent.com/CaBsCrypto/stellar-defi-quote-service/main/bazaar-listing.json",response=await fetch(url);assert.equal(response.status,200);const card=await response.json();assert.equal(card.slug,"stellar-defi-quote-service");assert.equal(card.capabilities?.[0]?.method,"POST");assert.equal(card.capabilities?.[0]?.path,"/api/quote");assert.equal(card.payment?.enabled,false);assert.equal(card.payment?.status,"FUTURE_X402_TESTNET_ONLY");assert.equal(card.networkAccess,false);console.log(JSON.stringify({ok:true,source:url,publicContract:true,endpointDeployed:false,paymentEnabled:false,mcpTransportPublished:false},null,2));
