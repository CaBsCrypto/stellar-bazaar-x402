# Environment Variables · Stellar Bazaar x402

> Nota en español: esta guía técnica está en inglés. Para la descripción general del proyecto en español, consulta [README.es.md](../README.es.md).

Every environment variable the app and test scripts read, where it comes from,
and how to treat it locally vs in production. Sources: `.env.example`,
`lib/x402-config.ts`, `lib/dynamic-registry.ts`, `lib/external-provider.ts`,
and the scripts listed below.

## Loading behavior

- **`.env.local`** — auto-loaded by Next.js. Never store payer/seller seeds
  here because build caches can capture values read during compilation.
- **`.env.x402.local`** — generated by `npm run x402:setup-wallets`
  (`scripts/setup-testnet-wallets.mjs`). **Not** auto-loaded by Next.js.
  Read explicitly by `scripts/test-x402-client.mjs`,
  `scripts/diagnose-x402-server.mjs`. Pass it only to a separately authorized
  local client process; never merge payer/seller seeds into `.env.local`.

## Table (`.env.example` + legacy + script-only vars)

| Variable | Purpose | Source | Local | Production |
|---|---|---|---|---|
| `STELLAR_X402_FACILITATOR_URL` | Facilitator endpoint; defaults to `https://channels.openzeppelin.com/x402/testnet` (`lib/x402-config.ts:9-10`) | Template / dashboard | Set | Set (testnet) |
| `STELLAR_X402_FACILITATOR_API_KEY` | Auth for verify/settle; `requireServerX402Config()` 503s without it (`lib/x402-config.ts:20-21`) | `npm run x402:fetch-facilitator-key` or facilitator dashboard | Set | Set |
| `X402_SELLER_ADDRESS` | Payment recipient; must match `/^G[A-Z2-7]{55}$/` | `npm run x402:setup-wallets` | Set | Set |
| `X402_PAYER_SECRET` | Client-side Testnet payer seed | ignored `.env.x402.local` only | Explicit client process only | **Never deploy/build** |
| `X402_RESOURCE_BASE_URL` | Loopback base for demo-pay (`lib/x402-config.ts:12-17`) and `test:x402:protocol` | Setup script writes `3210`; override to `http://127.0.0.1:3000` in `.env.local` | Set | Unused |
| `X402_ENABLE_LOCAL_PAYER` | Gates `POST /api/x402/demo-pay` (`app/api/x402/demo-pay/route.ts:12`) | Operator | `true` | `false` |
| `EXTERNAL_QUOTE_BASE_URL` | External quote service base; HTTPS or loopback (`lib/external-provider.ts:14-16`) | Operator | Optional | Optional |
| `RUN_EXTERNAL_X402_TESTNET` | Must be `"1"` to run `test:e2e:external:testnet`; script exits otherwise (`scripts/e2e-external-provider-testnet.mjs:1`) | Operator | `0` | `0` |
| `BAZAAR_ENABLE_REGISTRY_MUTATIONS` | Explicit kill switch for append-only HTTP registration; default/off unless exactly `true` | Operator | `false` | `false` until reviewed |
| `BAZAAR_PROVIDER_SECRET` | Server-to-server operator credential for `X-Bazaar-Provider-Key`; never an MCP/browser/provider-ownership credential | Operator | Unset | Required only when enabling registration |
| `UPSTASH_REDIS_REST_URL` | Upstash Redis URL for registry persistence (`lib/dynamic-registry.ts:18-20`) | Operator (Upstash) | Recommended | **Required (prod)** |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis token | Operator (Upstash) | Recommended | **Required (prod)** |
| `KV_REST_API_URL` | **Legacy** alias for `UPSTASH_REDIS_REST_URL` (`lib/dynamic-registry.ts:18`) | Vercel KV | Optional | Optional |
| `KV_REST_API_TOKEN` | **Legacy** alias for `UPSTASH_REDIS_REST_TOKEN` (`lib/dynamic-registry.ts:19`) | Vercel KV | Optional | Optional |
| `BASE_URL` | Target base for ecosystem / ingest / agent suites (default `http://127.0.0.1:3000`) | Script-only | Optional | Optional |
| `MCP_BASE_URL` | Target base for MCP suites (`scripts/test-mcp-onboarding.mjs:3`, `scripts/test-workflow-bundle.mjs:105`) | Script-only | Optional | Optional |

## Test scripts that read env

- `scripts/test-e2e-ecosystem.mjs:4` — `BASE_URL`.
- `scripts/test-mcp-onboarding.mjs:3,10` — `MCP_BASE_URL`, `BAZAAR_PROVIDER_SECRET`.
- `scripts/test-publisher-ingest.mjs:3,8` — `BASE_URL`, `BAZAAR_PROVIDER_SECRET`.
- `scripts/test-x402-protocol.mjs:4` — `X402_RESOURCE_BASE_URL` then `BASE_URL`.
- `scripts/e2e-external-provider-testnet.mjs:1-3` — `RUN_EXTERNAL_X402_TESTNET=1`,
  plus `EXTERNAL_QUOTE_BASE_URL` (must be HTTPS), `X402_PAYER_SECRET`,
  `STELLAR_X402_FACILITATOR_API_KEY`.
- `examples/agent-autonomous-buyer.mjs` — reads only `BASE_URL`; it performs
  discovery/policy preflight and stops before payment.

## Security notes

- Never deploy `X402_PAYER_SECRET` or `X402_SELLER_SECRET`; they are
  TESTNET-ONLY hot wallets (file headers in `.env.example` /
  `.env.x402.local`).
- Keep `BAZAAR_PROVIDER_SECRET` out of Git and browsers. There is no open mode.
- Registry POST requires the explicit mutation switch, an operator credential and
  Upstash. Update/delete/private listing stay disabled until per-provider
  ownership exists.
- Without Upstash the registry is read-only from public routes; the in-memory
  Map is useful for local read/test fixtures but cannot accept HTTP mutations.
- Run `npm run x402:rotate-payer` to replace only ignored local payer values
  and scrub payer plus seller-secret fields from auto-loaded `.env.local`. It prints the public
  address and readiness flags, never the seed. The new
  payer is unfunded until a separately authorized official Testnet setup.

## Demo-pay wiring (local only)

`POST /api/x402/demo-pay` works only when **all three** are in place
(`app/api/x402/demo-pay/route.ts:12-23`):

1. `X402_ENABLE_LOCAL_PAYER=true` (else `403 LOCAL_PAYER_DISABLED`);
2. `X402_PAYER_SECRET` set (else `503 PAYER_SECRET_MISSING`);
3. `X402_RESOURCE_BASE_URL` loopback — `http://127.0.0.1:3000` when the app
   runs on 3000 (else `400 LOCAL_PAYER_TARGET_REJECTED`, because
   `requireLocalResourceBaseUrl()` rejects non-loopback origins,
   `lib/x402-config.ts:12-17`).
